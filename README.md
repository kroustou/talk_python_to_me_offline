# Talk python to me episodes downloader
I needed to download these episodes to keep me company
so i wrote a script.

## Dependencies
`requests` and `bs4`

Installing with pip:

    pip install -r requirements.txt

